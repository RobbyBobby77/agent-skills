interface:
  display_name: "JSON, YAML, and TOML"
  short_description: "Edit config without destroying comments, types, or quotes"
  default_prompt: "Use $json-yaml to change the smallest structure in this file and re-parse it."
