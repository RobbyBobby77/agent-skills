interface:
  display_name: "JSON, YAML, and TOML"
  short_description: "Edit and validate structured configuration files"
  default_prompt: "Use $json-yaml to safely edit, convert, or validate this structured configuration."
