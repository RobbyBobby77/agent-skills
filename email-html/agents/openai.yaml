interface:
  display_name: "HTML Email"
  short_description: "Build HTML email that survives Outlook, Gmail, and dark mode"
  default_prompt: "Use $email-html to build a constrained HTML email with a matching text part for the named clients."
