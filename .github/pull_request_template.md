## Summary


## Checklist

- [ ] `python3 scripts/validate_skills.py` passes
- [ ] `python3 scripts/forward_test.py` passes
- [ ] `bash -n install.sh` passes
- [ ] `install.sh` stays portable (bash 3.2: no `mapfile`, `find -printf`, or `readlink -f`)
