interface:
  display_name: "Automated Testing"
  short_description: "Add tests that match CI and kill flakes at the source"
  default_prompt: "Use $testing to inspect this repo's runner and add the lowest reliable test that locks the behavior."
