interface:
  display_name: "HTTP APIs"
  short_description: "Change HTTP APIs without inventing a second contract"
  default_prompt: "Use $api to inspect the existing HTTP contract and make the smallest compatible change."
