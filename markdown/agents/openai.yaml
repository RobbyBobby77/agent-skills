interface:
  display_name: "Markdown"
  short_description: "Edit docs for the real renderer without rewriting the repo"
  default_prompt: "Use $markdown to change the smallest surface of this documentation so it matches nearby voice and actually renders."
