interface:
  display_name: "SQL"
  short_description: "Write safe SQL in the engine the database actually speaks"
  default_prompt: "Use $sql to name the dialect first and write a parameterized, authorized query."
