interface:
  display_name: "Docker"
  short_description: "Containerize and debug with the repo's real runtime"
  default_prompt: "Use $docker to inspect this repo's lockfile and produce or fix a multi-stage, non-root image."
